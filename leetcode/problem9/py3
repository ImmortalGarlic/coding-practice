class Solution:
    def isPalindrome(self, x):
        """
        :type x: int
        :rtype: bool
        """
        string = str(x)
        flag = True
        for i in range(len(string)):
            if string[i] != string[-(i+1)]:
                flag = False
        return flag
